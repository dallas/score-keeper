<%= form_for([@game, @game_play]) do |f| %>
  <% if @game_play.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_play.errors.count, "error") %> prohibited this game_play from being saved:</h2>

      <ul>
      <% @game_play.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :player_ids, 'Available Players' %><br />
    <%= f.collection_select :player_ids, Player.all.reject{|p| @game_play.player_ids.include?(p.id)}, :id, :name, {}, :multiple => true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>